# from django.urls import path
# from . import views


urlpatterns = [
    # path('backup-detail/<int:backup_id>/', views.backup_api_list.as_view()),
    # path('backups-list/', views.backup_api_list.as_view()),
    # path("download/<int:job_id>/", views.ManualBackup.as_view()),
]